name: Build PS2 ELF

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]
  workflow_dispatch:

jobs:
  build:
    name: Build PS2 Binary
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Install Host Build Essentials
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential wget curl libgmp-dev

      - name: Download and Setup PS2DEV Toolchain
        run: |
          export PS2DEV=/opt/ps2dev
          sudo mkdir -p $PS2DEV
          sudo chown -R $USER:$USER /opt/ps2dev

          curl -o ps2dev-latest.tar.gz -LC - https://github.com/ps2dev/ps2dev/releases/download/latest/ps2dev-ubuntu-latest.tar.gz
          tar -xf ps2dev-latest.tar.gz --strip-components 1 -C $PS2DEV

          echo "PS2DEV=/opt/ps2dev" >> $GITHUB_ENV
          echo "PS2SDK=/opt/ps2dev/ps2sdk" >> $GITHUB_ENV
          echo "/opt/ps2dev/bin" >> $GITHUB_PATH
          echo "/opt/ps2dev/ee/bin" >> $GITHUB_PATH

      - name: Build Project
        run: make -f Makefile.ps2

      - name: Upload ELF Artifact
        uses: actions/upload-artifact@v4
        with:
          name: gamate-elf
          path: gamate.elf